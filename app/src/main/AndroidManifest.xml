<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jinde.appfb02">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFB02">
        <activity
            android:name=".Admin.ListaMisPedidos_Activity"
            android:exported="false" />
        <activity
            android:name=".Cliente.RealizarPedido_Activity"
            android:exported="false" />
        <activity
            android:name=".Admin.Administar"
            android:exported="false" />
        <activity
            android:name=".Home_Activity"
            android:exported="false"
            android:parentActivityName=".AuthActivity" />
        <activity
            android:name=".AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>